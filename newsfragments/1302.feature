dials.image_viewer: when stacking N images, divide by N to put onto original 
scale, change command-line parameter from sum_images to stack_images to reflect
this, also add two modes for stacking - mean and max, with max as the default, 
which shows the highest pixel value in the stack. 
