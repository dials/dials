``dials.split_experiments`` is now called ``dials.split``, and ``dials.combine_experiments`` is now called ``dials.combine``, as they can work on both experiments and reflections. The old names are now deprecated.
