The test experiment for dials/tests/algorithms/profile_model/ellipsoid/test_refiner.py was created with a rotation but no goiniometer and as a ImageSet. This caused it to be misidentified as a still image in the test.
