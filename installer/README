To compile DIALS you need to run

  python bootstrap.py

which will install all dependencies to the directory 'conda_base' and then
configure and compile DIALS and cctbx.


Alternatively you can also run

  make

to the same effect


Once built you can load DIALS by running

  . build/setpaths.sh


For more information please go to https://dials.github.io/
